<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amalzoheir.hours">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterationActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".RegisterNewUserActivity"
            android:label="@string/Register" />
        <activity android:name=".ForgetPasswordActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".HomePageActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.amalzoheir.hours.HomeActivity" />
        </activity>
        <activity android:name=".ShowVideoActivity" android:parentActivityName=".HomePageActivity" android:label="@string/Video"/>
        <activity android:name=".PlayVideoActivity" android:parentActivityName=".HomePageActivity" android:label="@string/Video"/>
        <activity android:name=".LuxorDescripionActivity" android:parentActivityName=".HomePageActivity" android:label="@string/Luxor"/>
        <activity android:name=".BookTripsActivity" android:parentActivityName=".HomePageActivity" android:label="@string/Book"/>
        <activity android:name=".TripsSpicislNeedActivity" android:parentActivityName=".HomePageActivity" android:label="@string/TripsNeed"/>
        <activity android:name=".EgyptionTouristActivity" android:parentActivityName=".HomePageActivity"/>
        <activity android:name=".TherapeuticTourismActivity" android:parentActivityName=".HomePageActivity" android:label="@string/TherapeuticTourism"/>
        <activity android:name=".EgyptTouristMapActivity" android:parentActivityName=".HomePageActivity" android:label="@string/EgyptTouristMap"/>
        <activity android:name=".ProfileActivity" android:parentActivityName=".HomePageActivity" android:label="@string/Profile"/>
        <activity android:name=".MapActivity" android:parentActivityName=".HomePageActivity" android:label="@string/EgyptTouristMap"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".PyramidsVideoActivity" />
        <activity android:name=".GizaVideoActivity" />
        <activity android:name=".AswanVideoActivity" />
        <activity android:name=".AswanDetialsActivity"></activity>
    </application>

</manifest>