<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amalzoheir.hours">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterationActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".RegisterNewUserActivity"
            android:label="@string/Register" />
        <activity android:name=".ForgetPasswordActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".HomePageActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.amalzoheir.hours.HomeActivity" />
        </activity>
        <activity
            android:name=".ShowVideoActivity"
            android:label="@string/Video"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".PlayVideoActivity"
            android:label="@string/Video"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".LuxorDescripionActivity"
            android:label="@string/Luxor"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".BookTripsActivity"
            android:label="@string/Book"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".TripsSpicislNeedActivity"
            android:label="@string/TripsNeed"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".EgyptionTouristActivity"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".TherapeuticTourismActivity"
            android:label="@string/TherapeuticTourism"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".EgyptTouristMapActivity"
            android:label="@string/EgyptTouristMap"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/Profile"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".MapActivity"
            android:label="@string/EgyptTouristMap"
            android:parentActivityName=".HomePageActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".PyramidsVideoActivity" />
        <activity android:name=".GizaVideoActivity" />
        <activity android:name=".AswanVideoActivity" />
        <activity android:name=".AswanDetialsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LuxorPlaces" />
        <activity android:name=".Karnak" />
        <activity android:name=".TravelCompanies"></activity>
    </application>

</manifest>